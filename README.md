# 🎨 Arte Generativa com Turtle

Um projeto simples e criativo feito em **Python** usando a biblioteca **Turtle**, que gera uma espiral colorida e hipnotizante.  
Cada execução cria um padrão único, com cores aleatórias e curvas suaves.

---

## 🚀 Como funciona

O programa usa dois blocos principais:

1. **Configuração da tela (`configurar_tela`)**  
   - Cria a janela do Turtle com fundo preto.  
   - Define a velocidade, espessura e cor inicial do traço.

2. **Desenho da espiral (`desenhar_espiral`)**  
   - Escolhe cores aleatórias a cada passo.  
   - Desenha linhas e gira em um ângulo de 91°, formando uma espiral colorida.  
   - Aumenta gradualmente o comprimento das linhas, criando o efeito de expansão.

---

## 💻 Execução

### Pré-requisitos
- Ter o **Python 3** instalado.  
- A biblioteca **turtle** já vem incluída no Python padrão, então não é necessário instalar nada extra.

### Passos
1. Salve o código em um arquivo chamado `arte_turtle.py`.
2. Execute no terminal:
   ```bash
   python arte_turtle.py
